# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["python", "pbasf2", "pipe_tasks", "obs_subaru", "hscMosaic", "solvetansip"]
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasDoxygenInclude=False,
    hasSwigFiles=False,
)
