setupRequired(pipe_tasks)
setupRequired(obs_subaru)
setupRequired(hscAstrom)

setupOptional(pbasf2)
setupOptional(hscMosaic)
setupOptional(solvetansip)

pathPrepend(PATH, ${PRODUCT_DIR}/bin)
pathPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
