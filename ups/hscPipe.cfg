# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["pipe_tasks", "hscAstrom", ],
    "optional": ["pbasf2", "hscMosaic", "solvetansip", ]
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=[], libs=[],
    hasDoxygenInclude=False,
    hasSwigFiles=False,
)
